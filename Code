<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>EVENTY — Event‑агентство</title>
  <meta name="description" content="Современное event‑агентство: создаём мероприятия под ключ. Свадьбы, корпоративы, презентации и частные события." />
  <meta property="og:title" content="EVENTY — Event‑агентство" />
  <meta property="og:description" content="Современное event‑агентство: создаём мероприятия под ключ." />
  <meta property="og:type" content="website" />
  <meta name="theme-color" content="#0ea5e9" />
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🎉</text></svg>">
  <style>
    :root{
      --bg:#0b0b0f;        /* фон */
      --card:#11131a;      /* карточки */
      --muted:#9aa4b2;     /* приглушённый текст */
      --text:#e7eaf0;      /* основной текст */
      --accent:#0ea5e9;    /* акцент/кнопки */
      --accent-2:#22c55e;  /* вторичный акцент */
      --maxw:1120px;       /* ширина контейнера */
      --radius:18px;       /* скругления */
      --shadow:0 10px 30px rgba(2,6,23,.2);
    }
    *{box-sizing:border-box}
    html{scroll-behavior:smooth}
    body{margin:0; font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,'Helvetica Neue',Arial,'Noto Sans',sans-serif; color:var(--text); background:linear-gradient(180deg,#0b0b0f 0%, #0d1117 100%);}    
    a{color:inherit; text-decoration:none}
    .container{max-width:var(--maxw); margin-inline:auto; padding:24px}
    .nav{display:flex; align-items:center; justify-content:space-between; gap:16px}
    .logo{display:flex; align-items:center; gap:12px; font-weight:700; letter-spacing:.5px}
    .logo-mark{width:40px; height:40px; border-radius:12px; background:radial-gradient(120% 120% at 10% 10%, #38bdf8, #8b5cf6 60%, #22c55e); box-shadow:var(--shadow)}
    .btn{display:inline-flex; align-items:center; justify-content:center; gap:10px; padding:15px 21px; border-radius:14px; background:var(--accent); color:#001019; font-weight:700; border:1px solid rgba(255,255,255,.06); box-shadow:0 6px 20px rgba(14,165,233,.35); transition:transform .15s ease, filter .15s ease}
    .btn:hover{transform:translateY(-1px); filter:brightness(1.05)}
    .btn.secondary{background:transparent; color:var(--text); border-color:rgba(255,255,255,.12)}

    /* HERO */
    .hero{padding:64px 0 40px}
    .hero-wrap{display:grid; grid-template-columns:1.25fr .75fr; gap:32px; align-items:center}
    .eyebrow{display:inline-flex; align-items:center; gap:8px; padding:6px 10px; border:1px solid rgba(255,255,255,.12); border-radius:999px; color:var(--muted); font-size:14px}
    .title{font-size:clamp(34px, 5vw, 56px); line-height:1.05; margin:14px 0}
    .title strong{color:#fff}
    .lead{font-size:18px; color:#c7d0dc; max-width:48ch}
    .cta{display:flex; flex-wrap:wrap; gap:12px; margin-top:24px}
    .hero-card{background:linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02)); border:1px solid rgba(255,255,255,.1); border-radius:var(--radius); padding:24px; box-shadow:var(--shadow)}
    .hero-img{width:100%; height:100%; min-height:260px; border-radius:var(--radius); background:radial-gradient(120% 120% at 10% 10%, #1f2937, #0f172a); display:grid; place-items:center; position:relative; overflow:hidden}
    .hero-badge{position:absolute; top:16px; right:16px; background:#111827; color:#e5e7eb; padding:8px 12px; border-radius:999px; font-size:13px; border:1px solid rgba(255,255,255,.1)}

    /* ABOUT */
    section{padding:72px 0}
    .section-head{display:flex; align-items:end; justify-content:space-between; gap:16px; margin-bottom:28px}
    .section-title{font-size:28px; margin:0}
    .kpis{display:grid; grid-template-columns:repeat(3,1fr); gap:16px}
    .kpi{background:var(--card); border:1px solid rgba(255,255,255,.06); padding:20px; border-radius:16px}
    .kpi b{font-size:22px}
    .benefits{display:grid; grid-template-columns:repeat(3,1fr); gap:16px; margin-top:18px}
    .benefit{background:linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02)); border:1px solid rgba(255,255,255,.08); padding:18px; border-radius:16px}
    .benefit h4{margin:6px 0 8px}
    .muted{color:var(--muted)}

    /* PORTFOLIO */
    .grid{display:grid; grid-template-columns:repeat(3,1fr); gap:16px}
    .card{background:var(--card); border:1px solid rgba(255,255,255,.06); border-radius:16px; overflow:hidden}
    .card img{width:100%; height:220px; object-fit:cover; display:block}
    .ph{width:100%; height:220px; display:grid; place-items:center; border:2px dashed rgba(255,255,255,.18); background:transparent; color:var(--muted)}
    .card .cap{padding:14px 14px 16px}

    /* CONTACTS */
    .contact{display:grid; grid-template-columns:1fr 1fr; gap:24px}
    form{background:linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.02)); border:1px solid rgba(255,255,255,.1); padding:20px; border-radius:16px}
    .field{display:flex; flex-direction:column; gap:8px; margin-bottom:14px}
    .field input,.field textarea{border:1px solid rgba(255,255,255,.12); background:#0b1220; color:var(--text); padding:12px 14px; border-radius:12px; outline:none}
    .field input::placeholder,.field textarea::placeholder{color:#758195}
    .field textarea{min-height:120px; resize:vertical}
    .contacts-card{background:var(--card); border:1px solid rgba(255,255,255,.08); padding:20px; border-radius:16px}
    .list{display:grid; gap:12px}
    .list a{display:flex; align-items:center; gap:10px; padding:10px 12px; border:1px solid rgba(255,255,255,.08); border-radius:12px}

    footer{padding:28px 0 40px; color:var(--muted); border-top:1px solid rgba(255,255,255,.08)}

    /* RESPONSIVE */
    @media (max-width: 1000px){
      .hero-wrap{grid-template-columns:1fr}
      .contact{grid-template-columns:1fr}
      .grid{grid-template-columns:repeat(2,1fr)}
      .benefits,.kpis{grid-template-columns:repeat(2,1fr)}
    }
    @media (max-width: 620px){
      .grid,.benefits,.kpis{grid-template-columns:1fr}
      .hero{padding-top:28px}
      .section-head{flex-direction:column; align-items:flex-start}
    }
  </style>
</head>
<body>
  <!-- Header / Hero -->
  <header class="container">
    <nav class="nav" aria-label="Главная навигация">
      <a class="logo" href="#top">
        <span class="logo-mark" aria-hidden="true"></span>
        <span>EVENTY</span>
      </a>
      <div>
        <a class="btn secondary" href="#portfolio">Портфолио</a>
        <a class="btn" href="#contacts">Обсудить проект</a>
      </div>
    </nav>

    <section class="hero" id="top">
      <div class="hero-wrap">
        <div>          <h1 class="title"><strong>Event‑агентство</strong>, которое превращает задумку в событие, о котором говорят.</h1>
          <p class="lead">Свадьбы, корпоративы, презентации, частные вечеринки. Работаем под ключ: концепция, площадка, подрядчики, режиссура, продакшн и координация в день Х.</p>
          <div class="cta">
            <a class="btn" href="#contacts">Получить смету</a>
            <a class="btn secondary" href="#about">О нас</a>
          </div>
        </div>
        <div class="hero-card">
          <div class="hero-img" role="img" aria-label="Коллаж мероприятий">
            <svg width="220" height="160" viewBox="0 0 220 160" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <rect x="10" y="20" width="90" height="60" rx="10" fill="#0ea5e9" opacity=".9"/>
              <rect x="120" y="10" width="90" height="70" rx="10" fill="#22c55e" opacity=".85"/>
              <rect x="50" y="95" width="120" height="55" rx="10" fill="#8b5cf6" opacity=".85"/>
            </svg>
          </div>
        </div>
      </div>
    </section>
  </header>

  <!-- About -->
  <main>
    <section id="about">
      <div class="container">
        <div class="section-head">
          <h2 class="section-title">О нас</h2>
          <p class="muted">10+ лет в индустрии событий • Собственная база подрядчиков • Чёткие сроки и прозрачные сметы</p>
        </div>

        <div class="kpis" role="list">
          <div class="kpi" role="listitem"><b>350+ мероприятий</b><div class="muted">от камерных до на 1000 гостей</div></div>
          <div class="kpi" role="listitem"><b>97% рекомендаций</b><div class="muted">по итогам пост‑опросов</div></div>
          <div class="kpi" role="listitem"><b>24/7 координация</b><div class="muted">личный менеджер проекта</div></div>
        </div>

        <div class="benefits" role="list" aria-label="Преимущества">
          <article class="benefit" role="listitem">
            <h4>Под ключ</h4>
            <p class="muted">Берём на себя всё: от идеи и бюджета до монтажа, тайминга и послепродакшна.</p>
          </article>
          <article class="benefit" role="listitem">
            <h4>Сильный продакшн</h4>
            <p class="muted">Свет, звук, сцена, декор, арт‑номера — проверенные партнёры и единый стандарт качества.</p>
          </article>
          <article class="benefit" role="listitem">
            <h4>Прозрачно по деньгам</h4>
            <p class="muted">Фиксированные сметы, понятные договоры, отчёты по затратам.</p>
          </article>
        </div>
      </div>
    </section>

    <!-- Portfolio -->
    <section id="portfolio">
      <div class="container">
        <div class="section-head">
          <h2 class="section-title">Портфолио</h2>
          <p class="muted">Несколько недавних проектов</p>
        </div>

        <div class="grid">
          <!-- Плейсхолдеры: замените на реальные кейсы с фото и описанием -->
          <figure class="card">
            <div class="ph">Здесь будет кейс 1: фото + текст</div>
            <figcaption class="cap"><b>Кейс 1</b><div class="muted">Описание проекта появится здесь</div></figcaption>
          </figure>
          <figure class="card">
            <div class="ph">Здесь будет кейс 2: фото + текст</div>
            <figcaption class="cap"><b>Кейс 2</b><div class="muted">Описание проекта появится здесь</div></figcaption>
          </figure>
          <figure class="card">
            <div class="ph">Здесь будет кейс 3: фото + текст</div>
            <figcaption class="cap"><b>Кейс 3</b><div class="muted">Описание проекта появится здесь</div></figcaption>
          </figure>
        </div>
      </div>
    </section>

    <!-- Contacts -->
    <section id="contacts">
      <div class="container">
        <div class="section-head">
          <h2 class="section-title">Контакты</h2>
          <p class="muted">Расскажите о задаче — вернёмся с идеями и сметой в течение дня</p>
        </div>
        <div class="contact">
          <form id="contact-form" aria-labelledby="form-title">
            <h3 id="form-title" style="margin-top:0">Отправить заявку</h3>
            <div class="field">
              <label for="name">Имя</label>
              <input id="name" name="name" placeholder="Ваше имя" required />
            </div>
            <div class="field">
              <label for="phone">Телефон</label>
              <input id="phone" name="phone" inputmode="tel" placeholder="+7 (___) ___‑__‑__" required />
            </div>
            <div class="field">
              <label for="message">Сообщение</label>
              <textarea id="message" name="message" placeholder="Опишите мероприятие: дата, формат, бюджет…"></textarea>
            </div>
            <button class="btn" type="submit">Отправить</button>
            <p id="form-status" class="muted" style="margin-top:10px"></p>
          </form>
          <aside class="contacts-card">
            <h3 style="margin-top:0">Связаться напрямую</h3>
            <div class="list">
              <a href="tel:+77001234567" aria-label="Позвонить"><span>📞</span><span>+7 700 123‑45‑67</span></a>
              <a href="mailto:hello@eventy.agency" aria-label="Написать на email"><span>✉️</span><span>hello@eventy.agency</span></a>
              <a href="https://t.me/username" target="_blank" rel="noopener"><span>💬</span><span>Telegram</span></a>
              <a href="https://www.instagram.com/" target="_blank" rel="noopener"><span>📸</span><span>Instagram</span></a>
              <a href="https://wa.me/77001234567" target="_blank" rel="noopener"><span>🟢</span><span>WhatsApp</span></a>
              
            </div>
          </aside>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      © <span id="y"></span> EVENTY • Все права защищены • ИНН 000000000000
    </div>
  </footer>

  <script>
    // Подстановка года
    document.getElementById('y').textContent = new Date().getFullYear();

    // Простой обработчик формы (демо). Замените на свой backend или Formspree.
    const form = document.getElementById('contact-form');
    const statusEl = document.getElementById('form-status');
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      statusEl.textContent = 'Отправляем…';

      // Пример отправки как JSON (замените URL на ваш обработчик)
      try{
        const body = Object.fromEntries(new FormData(form).entries());
        // const resp = await fetch('https://your-backend.example/lead', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(body)});
        // if(!resp.ok) throw new Error('Ошибка сети');
        await new Promise(r=>setTimeout(r, 800)); // имитация запроса
        statusEl.textContent = 'Спасибо! Мы свяжемся с вами в ближайшее время.';
        form.reset();
      }catch(err){
        statusEl.textContent = 'Упс! Не получилось отправить. Попробуйте ещё раз или свяжитесь по телефону/мессенджеру.';
      }
    });
  </script>
</body>
</html>
s
